name: "Generate sitemap (disabled - manual only)"

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "This workflow has been disabled to prevent automatic sitemap generation."
          echo "Restore the previous workflow YAML from git history to re-enable."
